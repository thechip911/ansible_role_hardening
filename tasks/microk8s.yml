---
- name: Add path script
  become: true
  ansible.builtin.template:
    src: etc/tmp/microk8s.sh.j2
    dest: /tmp/microk8s.sh
    backup: true
    mode: 0644
    owner: root
    group: root
  tags:
    - kubes
    - M1038

- name: Install Kubes
  become: true
  ansible.builtin.command:
    cmd: sh /tmp/microk8s.sh
...